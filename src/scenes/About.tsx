import ScrollableBox from '../components/ScrollableBox';

type FocusedProps = {
    node: HTMLElement,
};

const About = (): JSX.Element => {
    return (
        <ScrollableBox />
    );
};

export default About;
